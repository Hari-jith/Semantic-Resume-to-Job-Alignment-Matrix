# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12          # required by FastAPI for file uploads

# --- Config / validation ---
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# --- ML / embeddings ---
sentence-transformers==3.2.1
torch>=2.6.0                       # CPU is fine for inference at this scale;
                                    # exact version depends on your Python version — pip will pick a compatible one

# --- File parsing ---
pdfplumber==0.11.4                 # PDF text extraction
python-docx==1.1.2                 # DOCX text extraction

# --- Skill extraction ---
spacy==3.8.2

# --- LLM ---
groq==0.11.0

# --- Utilities ---
tenacity==9.0.0                    # retry logic for Groq API calls
